import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zarx.system',
  appName: 'Zarx Security',
  webDir: 'dist/zarx-system/browser'
};

export default config;
